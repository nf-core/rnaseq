- name: Test STAR RSEM with skip_qc workflow parameters
  command: nextflow run main.nf -profile test --aligner star_rsem --skip_qc --outdir results
  tags:
    - star
    - rsem
    - skip_qc

- name: Test STAR RSEM with rsem_index false workflow parameters
  command: nextflow run main.nf -profile test --aligner star_rsem --rsem_index false --outdir results
  tags:
    - star
    - rsem
    - rsem_index-false
