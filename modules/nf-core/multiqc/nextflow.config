process {
    withName: 'MULTIQC' {
        ext.args = { [
            ((params.multiqc_title == null) ? '' : "--title \"${params.multiqc_title}\"")
        ].join(' ').trim() }
        ext.prefix = { meta.id == 'multiqc_report' ? "multiqc_report" : "${meta.id}_multiqc_report" }
        publishDir = [
            path: {
                "${params.outdir}/${task.ext.publish_prefix}multiqc" + (meta.id == 'multiqc_report' && !params.skip_alignment ? "/${params.aligner}" : "")
            },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }
}
