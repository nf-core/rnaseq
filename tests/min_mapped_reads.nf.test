nextflow_pipeline {

    name "Test pipeline with --min_mapped_reads 90"
    script "../main.nf"
    tag "RNASEQ"
    tag "PIPELINE"

    test("Parameters: --min_mapped_reads 90") {

        when {
            params {
                outdir = "$outputDir"
                min_mapped_reads = 90
            }
        }

        then {
            assert workflow.success
        }
    }
}
