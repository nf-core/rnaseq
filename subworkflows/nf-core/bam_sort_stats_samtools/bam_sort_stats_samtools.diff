Changes in component 'nf-core/bam_sort_stats_samtools'
'subworkflows/nf-core/bam_sort_stats_samtools/meta.yml' is unchanged
Changes in 'bam_sort_stats_samtools/main.nf':
--- subworkflows/nf-core/bam_sort_stats_samtools/main.nf
+++ subworkflows/nf-core/bam_sort_stats_samtools/main.nf
@@ -5,6 +5,19 @@
 include { SAMTOOLS_SORT      } from '../../../modules/nf-core/samtools/sort/main'
 include { SAMTOOLS_INDEX     } from '../../../modules/nf-core/samtools/index/main'
 include { BAM_STATS_SAMTOOLS } from '../bam_stats_samtools/main'
+
+record SamtoolsStatsFiles {
+    stats:    Path
+    flagstat: Path
+    idxstats: Path
+}
+
+record SortedBam {
+    id:       String
+    bam:      Path
+    bai:      Path
+    samtools: SamtoolsStatsFiles
+}
 
 workflow BAM_SORT_STATS_SAMTOOLS {
     take:
@@ -22,10 +35,23 @@
 
     BAM_STATS_SAMTOOLS(ch_bam_bai, ch_fasta_fai)
 
+    ch_results = ch_bam_bai
+        .map { meta, bam, bai -> [meta.id, bam, bai] }
+        .join(BAM_STATS_SAMTOOLS.out.results.map { r -> [r.id, r] }, by: [0])
+        .map { id, bam, bai, samtools ->
+            record(
+                id: id,
+                bam: bam,
+                bai: bai,
+                samtools: record(stats: samtools.stats, flagstat: samtools.flagstat, idxstats: samtools.idxstats)
+            ) as SortedBam
+        }
+
     emit:
     bam      = SAMTOOLS_SORT.out.bam // channel: [ val(meta), [ bam ] ]
     index    = SAMTOOLS_INDEX.out.index // channel: [ val(meta), [ index ] ]
     stats    = BAM_STATS_SAMTOOLS.out.stats // channel: [ val(meta), [ stats ] ]
     flagstat = BAM_STATS_SAMTOOLS.out.flagstat // channel: [ val(meta), [ flagstat ] ]
     idxstats = BAM_STATS_SAMTOOLS.out.idxstats // channel: [ val(meta), [ idxstats ] ]
+    results  = ch_results // channel: SortedBam
 }

'subworkflows/nf-core/bam_sort_stats_samtools/tests/main.nf.test.snap' is unchanged
'subworkflows/nf-core/bam_sort_stats_samtools/tests/main.nf.test' is unchanged
************************************************************
