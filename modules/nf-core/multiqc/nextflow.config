if (!params.skip_multiqc) {
    process {
        withName: 'MULTIQC' {
            ext.args = { [
                ((params.multiqc_title == null) ? '' : "--title \"${params.multiqc_title}\"")
            ].join(' ').trim() }
            ext.prefix = "multiqc_report"
        }
    }
}
