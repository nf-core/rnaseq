- name: subworkflow quantify_rsem
  command: nextflow run ./tests/subworkflows/local/quantify_rsem/ -entry test_quantify_rsem -c tests/config/nextflow.config
  tags:
    - quantify_rsem
  # TODO files:
