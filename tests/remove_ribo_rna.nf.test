nextflow_pipeline {

    name "Test pipeline with --remove_ribo_rna"
    script "../main.nf"
    tag "RNASEQ"
    tag "PIPELINE"

    test("Parameters: --remove_ribo_rna") {

        when {
            params {
                outdir = "$outputDir"
                remove_ribo_rna = true
            }
        }

        then {
            assert workflow.success
        }
    }
}
