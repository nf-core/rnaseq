process {
    withName: 'BRACKEN' {
        ext.args = "-l ${params.bracken_precision}"
        publishDir = [
            path: { "${params.outdir}/${task.ext.publish_prefix}${params.aligner}/contaminants/bracken" },
            mode: params.publish_dir_mode
        ]
    }
}
