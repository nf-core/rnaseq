nextflow_pipeline {

    name "Test pipeline with --skip_qc"
    script "../main.nf"
    tag "RNASEQ"
    tag "PIPELINE"

    test("Parameters: --skip_qc") {

        when {
            params {
                outdir = "$outputDir"
                skip_qc = true
            }
        }

        then {
            assert workflow.success
        }
    }
}
